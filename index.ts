// Web entry point — no Firebase or notifee (native-only).
// Native platforms (iOS/Android) use index.native.ts instead.

// eslint-disable-next-line import/first
import 'expo-router/entry';

// eslint-disable-next-line import/first
import './unistyles';

