import { SendNotification } from '@/features/admin/notification/components/send-notifications';
import React from 'react';

type Props = {};

const SendNotificationPage = (props: Props) => {
  return <SendNotification />;
};

export default SendNotificationPage;
