import { MyTitle } from '@/features/shared/components/my-title';
import { Wrapper } from '@/features/shared/components/wrapper';
import React from 'react';

const AccountType = () => {
  return (
    <Wrapper>
      <MyTitle title="Select account type" />
    </Wrapper>
  );
};

export default AccountType;
