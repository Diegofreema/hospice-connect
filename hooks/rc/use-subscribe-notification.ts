// Web stub — Firebase messaging and notifee are native-only packages.
// Native platforms (iOS/Android) use use-subscribe-notification.native.ts instead.
export const useSubscribeNotification = () => {
  // no-op on web
};

